[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cmc-csci040-valerie"
version = "0.1.0"
description = "A pirate chatbot"
readme = "README.md"

[project.scripts]
chat = "chat:repl"
